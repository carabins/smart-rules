import { N } from "avuef"

export default {
  nodes: {
    user: N,
    okx: N,
    dialogLogin: N
  },
  actions: {
    checkRune( ) {
      //if (!this.$f.session.rune.v) {
      //  this.$f.session.rune(newRune(34))
      //}
    }
  }
}
