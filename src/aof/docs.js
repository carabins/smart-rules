import { N } from 'avuef'

export default {
  nodes: {
    last: N.stored.start("x")
  },
  actions: {
    checkRune() {
      //if (!this.$f.session.rune.v) {
      //  this.$f.session.rune(newRune(34))
      //}
    }
  }
}
